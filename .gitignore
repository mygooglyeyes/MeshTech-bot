# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local configuration - each install copies config.example.yaml -> config.yaml
config.yaml

# Runtime data (database, logs)
data/*
!data/.gitkeep
*.db
*.db-wal
*.db-shm
*.log

# This workspace's private metadata (not part of the project)
.freebuff/
project-id

# Local dev/test scaffolding - not part of the shipped project
# (kept on each developer's machine; recoverable from git history)
tests/
requirements-dev.txt

# Editors / OS
.idea/
.vscode/
.DS_Store
